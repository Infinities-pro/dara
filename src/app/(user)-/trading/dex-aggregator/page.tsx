'use client';

import { LockedFeature } from '@/components/ui/locked-feature';

export default function DexAggregatorPage() {
  return (
    <LockedFeature
      iconName="arrow-left-right"
      title="DEX Aggregator"
      description="Find the best trading routes and prices across multiple decentralized exchanges with our advanced DEX aggregation service."
    />
  );
} 